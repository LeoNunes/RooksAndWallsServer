[Unit]
Description=Games Service

[Service]
Type=simple
EnvironmentFile=/etc/games/service.env
ExecStart=/usr/bin/java -jar /opt/games/service.jar
ExecStop=/bin/kill -15 $MAINPID
User=ec2-user
RemainAfterExit=no
Restart=on-failure
RestartSec=1

[Install]
WantedBy=multi-user.target